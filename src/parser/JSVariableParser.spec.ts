import {IUserPreferenceProfile, UserPreferenceProfile, UserProfileDelegate} from "../user/UserPreferenceProfile";

test('Media Descriptor Factory - parse #1',async () => {
    expect(true).toBe(true);
});


